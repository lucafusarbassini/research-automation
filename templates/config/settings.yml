# Project configuration
# Generated by: research init

project:
  name: ""
  type: "ml-research"
  created: ""

compute:
  type: "local-cpu"
  gpu: ""

notifications:
  enabled: false
  method: "none"

credentials:
  # References to environment variables, not actual values
  github: "${GITHUB_TOKEN}"

# Customize these for your project
preferences:
  auto_commit: true
  checkpoint_interval: 30  # minutes
  max_overnight_iterations: 20
